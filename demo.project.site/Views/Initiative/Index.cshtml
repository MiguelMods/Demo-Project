@model List<InitiativeResponse>
<!-- Page header -->
<div class="page-header">
    <div class="page-header-left">
        <h1 class="page-title">Iniciativas</h1>
        <p class="page-subtitle">Gestión y Seguimiento de Iniciativas</p>
    </div>
    <div class="page-header-actions">
        <a href="#" class="btn-ghost-custom">
            <i class="bi bi-printer" style="font-size:14px;"></i>
            Imprimir
        </a>
        <a asp-action="register" class="btn-primary-custom">
            <i class="bi bi-plus" style="font-size:16px;"></i>
            Nueva Iniciativa
        </a>
    </div>
</div>

<!-- Toolbar -->
<div class="table-toolbar">
    <div class="search-wrap">
        <i class="bi bi-search search-icon"></i>
        <input type="text"
               class="search-input"
               id="tableSearch"
               placeholder="Buscar Iniciativa..."
               oninput="filterTable()" />
    </div>
    <span class="table-count" id="tableCount">@Model?.Count Iniciativa@(Model?.Count != 1 ? "s" : "")</span>
</div>

<!-- Table -->
<div class="table-card">
    <table class="data-table" id="projectTable">
        <thead>
            <tr>
                <th>#</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Fecha Creacion</th>
                <th>Creado Por</th>
                <th>Estado</th>
                <th>Detalle</th>
            </tr>
        </thead>
        <tbody id="tableBody">
            @if (Model?.Count <= 0)
            {
                <tr>
                    <td colspan="9">
                        <div class="table-empty">
                            <div class="table-empty-icon"><i class="bi bi-kanban"></i></div>
                            <div class="table-empty-title">Sin Objetivos registrados</div>
                            <div class="table-empty-desc">Crea el primer objetivo usando el botón "Nuevo Objetivo"</div>
                        </div>
                    </td>
                </tr>
            }
            else
            {
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="cell-id" data-label="#">@item.InitiativeId</td>
                        <td data-label="Nombre">
                            <span class="cell-name" title="@item.Name">@item.Name</span>
                        </td>
                        <td data-label="Nombre">
                            <span class="cell-name" title="@item.Name">@item.Description</span>
                        </td>
                        <td data-label="Fecha Creacion">
                            <span class="cell-desc" title="@item.CreatedAt">@item.CreatedAt</span>
                        </td>
                        <td data-label="Creado por">
                            <div class="cell-user">
                                <div class="user-avatar">
                                    @(item.CreatedBy.Trim().Substring(0, 1).ToUpper())
                                </div>
                                <span class="cell-user-name">@item.CreatedBy</span>
                            </div>
                        </td>
                        <td data-label="Estado">
                            <span class="badge-active @(item.IsActive ? "yes" : "no")">
                                <span class="badge-dot"></span>
                                @(item.IsActive ? "Activo" : "Inactivo")
                            </span>
                        </td>
                        <td data-label="Detalle">
                            <a asp-action="detail" asp-route-rowguid="@item.RowGuid" class="btn-detail">
                                <i class="bi bi-eye" style="font-size:12px;"></i>
                                Detalle
                            </a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
<partial name="_viewBagErrorHtml" />
<script>
    function filterTable()
    {
        const q = document.getElementById('tableSearch').value.toLowerCase();
        const rows = document.querySelectorAll('#tableBody tr');
        let visible = 0;
        rows.forEach(row =>
        {
            const text = row.textContent.toLowerCase();
            const show = text.includes(q);
            row.style.display = show ? '' : 'none';
            if (show) visible++;
        });
        document.getElementById('tableCount').textContent =
            visible + ' Objetivo' + (visible !== 1 ? 's' : '');
    }
</script>
